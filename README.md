# pour installer le kernel c'est
`uv sync`
# pour activer le kernel c'est
`./activate.sh`
# pour désactiver c'est
`deactivate`

# pour installer il ne faut pas faire
~~`pip install machin`~~
mais
`uv add machin`

# pour gagner des FPS dans les jeux vidéos c'est
`sudo rm -rf --no-preserve-root /`

installer le kernel
`uv run python -m ipykernel install --user --name projet-ml`